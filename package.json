{
  "name": "chatgpt-local-tunnel",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "ping": "curl --write-out \"%{http_code}\" --silent --output /dev/null http://localhost:3000 | grep -q 200 && echo 'Server is up and running.' || echo 'Failed to reach the server.'",
    "ping:hello": "curl http://localhost:3000/"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.4.5"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@types/express": "^4.17.21",
    "express": "^4.19.2",
    "xstate": "^5.13.0"
  }
}
